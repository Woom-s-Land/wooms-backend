services:
  backend:
    container_name: backend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - ./env_file
    networks:
      - wooms

networks:
  wooms:
    external: true
